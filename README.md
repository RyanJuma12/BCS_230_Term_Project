# BCS_230_Term_Project
Note Project Was Made using CLion so the executable file is in the Cmake-Build-Debug folder. You might need to update include paths if using VS Code or Microsoft Visual Studio.
